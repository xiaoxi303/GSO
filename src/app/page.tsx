import MainDashboard from "@/components/MainDashboard";

export const metadata = {
  title: "Global Market Intelligence - Command Dashboard",
  description: "Real-time intelligence dashboard monitoring global flows, index performance, news triggers and critical system risks."
};

export default function Home() {
  return <MainDashboard />;
}
